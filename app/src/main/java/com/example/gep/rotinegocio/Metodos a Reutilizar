public class PedidoAdapter extends RecyclerView.Adapter<PedidoAdapter.MyViewHolder> {


    @Override
    public MyViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {
        View view = LayoutInflater.from(parent.getContext())
                .inflate(R.layout.card_view_pantalla_pedidos, parent, false);
        MyViewHolder mVH=new MyViewHolder(view);

        return mVH;

    }

    @Override
    public void onBindViewHolder(MyViewHolder holder, int position) {

    }

    @Override
    public int getItemCount() {
        return 0;
    }

    public class MyViewHolder extends RecyclerView.ViewHolder implements View.OnClickListener{


//        ACÁ SE DECLARAN LOS TEXTVIEW Y DEMACES
        public MyViewHolder(View itemView){
            super(itemView);
//            ACÁ INSTANCIAR TODOS LAS VISTAS

        }

        @Override
        public void onClick(View v) {

        }
    }
}


ADAPTER PARA RECYCLER
----------------------------------------------------------------------------------------------------------